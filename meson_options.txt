option('enable_asm',
    type: 'boolean',
    value: true,
    description: 'Build asm files, if available')

option('libwebp',
    type: 'combo',
    choices: ['auto', 'subproject', 'system', 'disabled'],
    value: 'auto',
    description: 'Where the libwebp compared against comes from')

option('libwebpdecoder',
    type: 'string',
    value: '',
    description: 'Path to a static libwebpdecoder.a for libwebpdec')

option('testdata_tests',
    type: 'boolean',
    value: false,
    description: 'Run tests requiring the wpd-test-data directory')

option('testdata_dir',
    type: 'string',
    value: '',
    description: 'Path to wpd-test-data; defaults to wpd-test-data in the source root')

option('trim_dsp',
  type: 'combo',
  choices: ['true', 'false', 'if-release'],
  value: 'if-release',
  description: 'Eliminate DSP functions the build target cannot reach')
